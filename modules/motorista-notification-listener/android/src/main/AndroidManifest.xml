<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <service
      android:name=".RideNotificationListenerService"
      android:label="MotoristaPro — leitura de ofertas"
      android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
      android:exported="false">
      <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
      </intent-filter>
    </service>

    <service
      android:name=".RideAccessibilityService"
      android:label="MotoristaPro — captura de ofertas na tela"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
      android:exported="true">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/motorista_accessibility_service" />
    </service>

    <service
      android:name=".Ride99AccessibilityService"
      android:label="MotoristaPro — captura 99"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
      android:exported="true">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/motorista_99_accessibility_service" />
    </service>

    <service
      android:name=".RideLifecycleAccessibilityService"
      android:label="MotoristaPro — ciclo e recebimento da corrida"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
      android:exported="true">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/motorista_ride_lifecycle_accessibility_service" />
    </service>
  </application>
</manifest>
